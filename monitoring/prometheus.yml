global:
  scrape_interval: 5s      # Scrape every 5 seconds for responsive graphs
  evaluation_interval: 5s

scrape_configs:
  - job_name: "gateway"
    static_configs:
      - targets: ["gateway:8080"]
    metrics_path: /metrics
